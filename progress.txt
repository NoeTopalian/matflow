# Polish-Pack Sprint — Progress Log

## Session start
- Base commit: b28a8ed (payments inbox + name truncate)
- Build state: green
- Vitest: broken (deferred per RB-001 from prior Ralph)
- Schema finding: Member has NO totpSecret/totpEnabled fields → PP-002 scope reduced

## Iterations

### PP-001 — refund refinements
Status: SHIPPED as 176caa0
Files: components/dashboard/PaymentsTable.tsx + app/api/payments/[id]/refund/route.ts
Modal replaces window.confirm; partial amount + reason fields; backend voids
class-pack credits + adds packVoided to audit metadata.

### PP-002 + PP-003 — 2FA Settings card improvements (batched)
Status: SHIPPED as 6bff707
File: components/dashboard/SettingsPage.tsx (single file, both stories)
- Regenerate codes button → modal with 8 fresh codes (Copy / Download / ack-tickbox)
- Manage 2FA link routes to /login/totp/setup for re-enrolment
- Member 2FA opt-in deferred (Member schema lacks totpSecret/totpEnabled —
  needs migration; documented as follow-up)
Recovery-codes API route already supported repeat invocation; no backend change.

All 3 stories complete. tsc + build green between each commit.

---

## Ralph Ultimate Test Suite — current state (2026-05-10)

Spec: .omc/specs/deep-dive-matflow-ultimate-test-suite.md
PRD: .omc/prd.json (stories US-001..006)
Trace: .omc/specs/deep-dive-trace-matflow-ultimate-test-suite.md

### Stories complete (this session)
- US-001 ✓ Reviewer prompt + learnings.md (.omc/ralph/{reviewer-prompt.md, learnings.md})
- US-002 ✓ 3 planted-bug probes — all caught (tenant-scope-bypass / expectation-tuning / tautology)
- US-003 ✓ Error catalogue at .omc/ralph/error-catalogue.json — 80 items (22 CRITICAL / 36 HIGH / 19 MEDIUM / 3 LOW), all status: open
- US-004 ✓ Mode A test infra + cross-tenant suite:
  - .env.test.example (template + Neon branch instructions)
  - tests/setup-test-db.ts (refuses prod endpoint id; strips DATABASE_URL when no TEST_DATABASE_URL)
  - vitest.config.ts (setupFiles wired)
  - tests/integration/cross-tenant-authorisation-matrix.test.ts (Member fully implemented; 10 models stubbed for mechanical expansion)
  - tests/integration/README.md (one-time Neon branch setup)
- US-005 ✓ This handoff section

### Stories pending
- US-006: per-catalogue-item Ralph loop until stop predicate. ~80 open items remain.

### How to resume
1. Provision a Neon branch (one-time). See tests/integration/README.md.
2. Set TEST_DATABASE_URL in .env.test (gitignored).
3. Re-invoke: `/ralph .omc/specs/deep-dive-matflow-ultimate-test-suite.md`
4. Ralph will pop CRITICAL items first, write tests, run reviewer prompt per iteration.

### Estimated cost for one full pass
- LLM: ~$32 (Sonnet) / ~$200 (Opus) for 80 iterations × executor + reviewer
- Neon branch: ~$0.20 in compute (16 milestones × ~5 min ephemeral)
- Vercel: zero (loop is local; CI gate unchanged)
- Wall-clock: ~6 hours autonomous

### Cheapest highest-value next iteration
ULT-001 (class capacity overrun, CRITICAL) — atomic UPDATE pattern from defensive-eng §3.4. Spawn 5 parallel Promise.all bookings against capacity=1 instance, assert exactly 1 succeeds. Catches a documented race-condition class.

---

## Loop session 2 (2026-05-11) — 3 iterations, 4 bugs fixed

iter-1 (commit f92b918): DELETE /api/members/[id] silent-200 fix. Found by the parameterised cross-tenant suite — DELETE returned 200 even when count===0 (cross-tenant member). Fix: check result.count, return 404 if zero. No data leak (tenant filter was correct), but inconsistent HTTP semantics with GET/PATCH.

iter-2 (commit e0ba524): Many-style mutator audit — 3 more of the same bug pattern caught and fixed in the [id]/route.ts handlers for announcements (DELETE), classes (DELETE soft-delete), initiatives/attachments (DELETE). 10 other [id] mutator handlers were already correct.

iter-3 (commit 6fe1a93): Kiosk-path pack credit race in lib/checkin.ts (lines 220-246) — used findFirst → unguarded update.decrement instead of the atomic updateMany guard the self-path uses. Two parallel kiosk check-ins for the same member with the same pack could decrement credits to -1. Fix: copy the self-path's atomic pattern. Regression test at tests/integration/checkin-pack-race.test.ts. ULT-001 reframed as N/A (no booking/capacity system exists in MatFlow — only check-in).

### Net for this session
- 4 production bugs fixed (3 silent-200 inconsistencies + 1 concurrency race)
- 2 new integration tests (cross-tenant matrix + pack race)
- Mode A test infrastructure live (Neon test branch wired)
- 3 catalogue items processed (ULT-066 NEW pattern audit, ULT-001 reframed wontfix, plus the implicit Member item iter-1 closed)
- 77 catalogue items remain open

### Resume next session
1. Wake the Neon test branch (`TEST_DATABASE_URL=... npx vitest run tests/integration/cross-tenant-authorisation-matrix.test.ts` — first run cold-fails, second succeeds).
2. Re-invoke `/ralph .omc/specs/deep-dive-matflow-ultimate-test-suite.md`.
3. Likely starting points (high-leverage CRITICAL items): ULT-005 (kiosk → admin escalation), ULT-008 (duplicate belt promotion no-undo), ULT-009 (magic-link replay), ULT-013 (member self-elevate to coach).
4. Or extend cross-tenant suite to the remaining 9 stubbed models (Payment, RankSystem, Class, User-staff, MembershipTier, etc.).

### Stale-primer corrections discovered this session
- Cursor primer claimed "no GitHub Actions CI" — wrong, .github/workflows/ci.yml exists (typecheck + Vitest with continue-on-error). Lane 2 of the trace flagged this; the spec drops priority #6 from the working list.
- Live Stripe `rk_live_*` keys are in .env (committed?) — verify scopes in Stripe dashboard before any Stripe-touching iteration.
- DATABASE_URL in .env points at PROD Neon. The new setup-test-db.ts gate refuses to run integration tests against it.

### RB-001 strategy (decided)
Catalogue items ULT-RB-001..011 are gap-stubs. Iteration N for RB-001 should run `npm test 2>&1 | grep -E "(FAIL|✗)" | head -20` first to enumerate exact failing test names, then patch the catalogue entries with real route_or_module values before fixing.

