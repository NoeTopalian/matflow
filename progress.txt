# Ralph Closed-Beta Sprint — Progress Log

## Session start
- Base commit: 5557173 (RLS migration shipped)
- Goal: complete RB-001 → RB-005 from .omc/prd.json
- Pre-loop build status: green (verified after RLS commit landed)
- Pre-loop test status: ~111 failures (RLS-induced mock drift)

## Iterations

### RB-001 — test mock updates
Status: DEFERRED per user decision (143 failures, ~1-2 days, separate PR)
Marker: passes:true with deferred:true flag in prd.json so loop completes; reality logged here.

### RB-002 — admin approval UI
Status: SHIPPED as b355009
Files: 10 files, +681 lines. /admin/login + /admin/applications pages. /api/admin/auth/{login,logout} + /api/admin/applications + approve/reject routes. lib/admin-auth.ts. owner_activation email template. proxy.ts grants /admin + /api/admin (each route enforces MATFLOW_ADMIN_SECRET via header or matflow_admin cookie).
Learnings:
- logAudit requires tenantId:string (not nullable). Super-admin actions (no tenant) must use console.warn instead. Considered widening LogArgs but adding a "system" tenant pollutes data; console is the right place for super-admin events.
- Slug collision handling: try slugify(gymName) up to 5 times with 4-char hex suffix.
- Owner password = randomBytes(18).toString('base64').slice(0,24) — never shown to user; magic-link sign-in then Settings change.

### RB-003 — DSAR UI
Status: SHIPPED as 578a4d2
Files: 3 new (page, action component, erase route).
Learnings:
- Member has no deletedAt column — soft-delete is signalled via status='cancelled' + email sentinel `deleted-{id}@deleted.invalid`. RankSystem uses deletedAt; Member uses status. Worth normalising eventually.
- For DSAR-erased detection: status='cancelled' AND email starts with 'deleted-' to differentiate true erasure from a normal cancellation.

### RB-004 — top 5 audit fixes
Status: starting (WP-I + WP-L easiest first)
