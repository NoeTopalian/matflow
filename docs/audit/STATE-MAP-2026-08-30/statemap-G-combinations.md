# MatFlow State Map — Lane G: COMBINATION HUNTER

Read-only audit of `C:/Users/NoeTo/Desktop/matflow`. Every cell below is a
**combination** of two or more states whose joint behaviour is not visible from
either axis alone. Cells already recorded in `statemap-A-identity.md`,
`statemap-B-tenant-billing.md`, `statemap-C-roles-settings.md` or
`statemap-D-data-lifecycle.md` are treated as KNOWN and excluded, unless a prior
lane claim is materially wrong — in which case the cell is flagged
**CORRECTS-<lane>**. Every claim carries file:line. Anything reasoned but not
proven is tagged **UNVERIFIED**. Nothing was modified; no dev server, test or
database was run.

---

## 0. HEADLINE TRUTHS — the worst 12 new cells

| # | Combination | What actually happens | Evidence |
|---|---|---|---|
| **G-1** | **Partial refund × class pack** | Refunding **any** amount — £5 of a £100 ten-class pack — sets `status: "refunded", creditsRemaining: 0` on the whole pack. There is no `fullyRefunded` guard on the pack branch, in either the API route or the webhook. The member loses every credit for a 5% refund, and `restorePackCreditsForAttendance` then refuses to hand credits back on a refunded pack, so the loss is permanent | `app/api/payments/[id]/refund/route.ts:313-322` — the guard is only `refundSettled && payment.stripePaymentIntentId`, while `fullyRefunded` is computed at `:244` and used only at `:307`; identical bug in `app/api/stripe/webhook/route.ts:645-657`; no-restore rule `lib/checkin.ts:102` |
| **G-2** | **Staff-cancelled member × live Stripe subscription × self check-in** | `PATCH /api/members/[id]` with `status: "cancelled"` calls Stripe `cancel_at_period_end` and then writes **only** `status` + `cancelledAt`. It never touches `stripeSubscriptionId` or `paymentStatus`. `lib/checkin.ts:195-196` derives coverage as `!!stripeSubscriptionId && paymentStatus === "paid"` — both still true. A cancelled member therefore self-checks-in with **full subscription coverage** (not pack credits, not a comp) for up to a whole billing period | write set `app/api/members/[id]/route.ts:313-320`; coverage rule `lib/checkin.ts:195-196`; the self path applies no status filter `app/api/checkin/route.ts:103-113` |
| **G-3** | **`ClassWaitlist` x every surface that reads it** | The model has **zero create sites anywhere in the repo**. Its only writers are three `deleteMany` calls. Yet the coach register renders a full waitlist panel, `coach/today` returns `waitlistCount`, and `DELETE /api/classes/[id]` **refuses to delete class instances that have waitlist rows**. An entire feature slice, including a destructive-action safety refusal, reads a table nothing can populate | writers: `lib/member-delete.ts:123` and `app/api/cron/retention/route.ts:612`, both deletes. Readers: `app/api/coach/instances/[id]/register/route.ts:53`, `app/api/coach/today/route.ts:38,70`, `app/api/classes/[id]/route.ts:167,175,178`, `components/dashboard/CoachRegister.tsx:293-303`, `components/dashboard/TimetableManager.tsx:78`. Grep for classWaitlist.create across app, lib, scripts, prisma returns nothing |
| **G-4** | **Two staff editing the same member (or staff row) at once** | An optimistic-concurrency precondition exists — `updateMany` guarded on `updatedAt` — in exactly two routes, and it is **opt-in**: `const concurrencyGuard = clientUpdatedAt ? { updatedAt: … } : {}`. **No client in the repository ever sends it.** Every concurrent staff edit is silently last-write-wins and the 409 branch is unreachable in production | `app/api/members/[id]/route.ts:231,314`; `app/api/staff/[id]/route.ts:69,90`; grep for `clientUpdatedAt`, or an `updatedAt` key inside any fetch body, across `app/**/*.tsx` and `components/**/*.tsx` returns **zero** senders |
| **G-5** | **Kid × promote-to-adult × bulk-invite** | `promote-to-adult` writes only `accountType: "adult", parentMemberId: null`. It leaves the synthesised `kid-<hex>@no-login.matflow.local` address and `passwordHash: null` in place. `bulk-invite` excludes candidates only by `accountType: { not: "kids" }`, so the promoted row is now **eligible**, and a club-wide invite run performs a real Resend send to an RFC-2606 reserved `.local` TLD. One guaranteed hard bounce per promoted kid | promote write set `app/api/members/[id]/promote-to-adult/route.ts:41-44`; address format `lib/synthesise-kid-email.ts:19`; eligibility filter `app/api/members/bulk-invite/route.ts:54-63`; `sendEmail` has **no** synthetic-address guard `lib/email.ts:345-385` |
| **G-6** | **DSAR-erased member × in-flight `checkout.session.completed`** | The class-pack branch resolves the buyer by `metadata.memberId`, not `stripeCustomerId`, and validates only existence plus tenant. Erasure is a scrub, not a delete, so the row is still there. A pack checkout that completes after the erase **creates a new `MemberClassPack`, a `succeeded` `Payment`, and queues a receipt email to `deleted-<id>@deleted.invalid`**. Row re-creation on an Article-17-erased subject, plus a guaranteed bounce | `app/api/stripe/webhook/route.ts:429-442` member lookup with no status or sentinel check, creates at `:451-484`, receipt to `packBuyer.email` at `:486-500`; sentinel format `app/api/admin/dsar/erase/route.ts:158` |
| **G-7** | **DSAR-erased member × `charge.dispute.*`** | `findMember()` returns null because the erase nulled `stripeCustomerId`, but the fallback is `linkedPayment?.memberId` — and DSAR erase **does not touch `Payment` rows at all**. So the dispute branch resolves the erased member and runs `tx.member.update({ data: { paymentStatus: "overdue" } })` on the erased row, then writes an `AuditLog` keyed `entityType: "Member", entityId: <erased id>`. The supposedly inert row moves again, surfaces in the payments-due tile as "Deleted member", and that audit row is later returned by the same member SAR export | fallback `app/api/stripe/webhook/route.ts:962`, write `:966-969`, audit `:975-990`; the complete erase sweep is `app/api/admin/dsar/erase/route.ts:276-393` and contains no `payment.` call |
| **G-8** | **DSAR-erased parent × active kids** | `dsar/erase` contains **no reference to `parentMemberId` or `children` whatsoever**. The kids stay `status: "active"` with `parentMemberId` pointing at a row that is now `"Deleted member"`, `status: "cancelled"`, `passwordHash: null`, `sessionVersion` bumped. Therefore the parent can never sign in, so `POST /api/checkin` with `onBehalfOfMemberId` is unreachable; and the kiosk excludes the parent from search (`status IN (active, taster)`), so the only kiosk route to those kids — the parent tap — is gone. The kids remain live and billable but unattendable | grep parentMemberId and children over `app/api/admin/dsar/erase/route.ts` returns **no matches**; erase write set `:277-312`; parent-tap gate `app/api/checkin/route.ts:86-95`; kiosk parent filter `app/api/kiosk/[token]/members/route.ts:57-59`, kid list `:80-81` |
| **G-9** | **Suspended or soft-deleted tenant × pending invite / reset token** | Neither `POST /api/members/accept-invite` nor `POST /api/auth/reset-password` reads `Tenant.deletedAt` or `Tenant.subscriptionStatus`. Both **burn the single-use token, write the new `passwordHash` and bump `sessionVersion`** — and only afterwards does `auth.ts:193-194` refuse the sign-in. The account is activated into a state it can never use, the token is spent, and re-minting requires staff who are themselves locked out | accept-invite performs no tenant-state read anywhere in `app/api/members/accept-invite/route.ts:51-107`; reset-password reads the tenant at `app/api/auth/reset-password/route.ts:37-48` and checks only existence, writes at `:154-193`; the gate that then refuses is `auth.ts:193-194` |
| **G-10** | **Locked account × password reset or invite accept** | `ACCOUNT_LOCKOUT_THRESHOLD = 10` sets `lockedUntil = now + 1h`. Neither `reset-password` nor `accept-invite` clears `lockedUntil` or `failedLoginCount`; the **only** writer that clears them is a *successful* login, which is precisely what the lock prevents. `auth.ts:222` evaluates `isLocked` before the password comparison, so the user changes their password successfully and is still refused for the full hour | constants `auth.ts:105-106`; clear-on-success only `auth.ts:286-297`; lock ordering `auth.ts:222,241`; reset write `app/api/auth/reset-password/route.ts:160-166,186-192`; invite write `app/api/members/accept-invite/route.ts:81-84` |
| **G-11** | **Overdue kid × parent pays × self vs kiosk** | Kids get their own Stripe customer, created with `email: kid.email` — the synthetic `.local` address. On `invoice.payment_failed` the webhook flips the **kid** to `overdue` and queues `payment_failed` **to that `.local` address**. The owner fan-out sits inside the same `if (memberFull?.email)` block so owners are told, but **the parent who is actually paying is notified by nothing at all**. The kid is then refused on the parent phone (`onBehalfOf` inherits the `self` profile, `requireCoverage: true`, 402) and admitted at the kiosk (`requireCoverage: false`) | customer email `app/api/member/subscriptions/start-for-kid/route.ts:133-140` feeding `lib/stripe/subscriptions.ts:82-84`; dunning `app/api/stripe/webhook/route.ts:307-326`; parent-path profile `app/api/checkin/route.ts:96-99` and `:123-126`; kiosk profile `app/api/kiosk/[token]/checkin/route.ts:81` |
| **G-12** | **Kiosk × any member not exactly `paymentStatus: "paid"` × class pack** | The kiosk passes `requireCoverage: false`, so the member is admitted regardless. `performCheckin` then runs an *opportunistic* redemption gated only on `method === "kiosk" && !hasActiveSubscription`, and `hasActiveSubscription` is false for `overdue`, `paused`, `pending` **and `free`**. An overdue, paused or comped member holding a prepaid pack silently burns a credit for a check-in that required no coverage at all. The pack drains to zero while the member believes they are on a subscription or a comp | `lib/checkin.ts:283-315`; coverage definition `:195-196`; legal paymentStatus values `prisma/migrations/20260430000001_schema_check_constraints/migration.sql:23` |

Three runners-up that only just missed the top twelve, and would displace
several of them on a different weighting: **G-31** (a waiver text change forces
no re-sign and `SignedWaiver.version` has zero readers — the club cannot prove
who agreed to which text), **G-40** (re-running `bulk-invite` against a
bounce-suppressed address destroys the member's only live invite token and
mints an undeliverable replacement — the most likely first-day failure for a
CSV-imported club), and **G-25** (a lost chargeback leaves the subscription
billing).

---

## 1. Combination table

Severity is blast radius times likelihood for a real club, not code severity.
Rows marked **KNOWN-EXT** extend a prior lane's finding with a materially
different consequence; rows marked **CORRECTS-x** contradict it.

### 1.1 Membership lifecycle × check-in path

| # | State A | State B | Surface | Behaviour | Evidence | Severity |
|---|---|---|---|---|---|---|
| G-13 | `Member.status = "cancelled"` | active class pack with credits | `POST /api/checkin` (self) vs kiosk | **Asymmetric.** Self check-in succeeds (no status filter anywhere in the resolve-or-gate path) and redeems the pack. The kiosk **cannot find them at all** — the roster search filters `status IN (active, taster)` — so the same paid credits are spendable from the member phone and unusable at the front door. Nothing in either UI explains the difference | self resolve `app/api/checkin/route.ts:103-113`; gate profile `:123-126`; `lib/checkin.ts` never reads `status`; kiosk filter `app/api/kiosk/[token]/members/route.ts:57-59` | **Medium** (KNOWN-EXT of B1 — B1 records that credits survive, not that the two doors disagree) |
| G-14 | cancelled or GDPR-erased member | `ClassRoster` rows | roster gate, roster UI, class edit | Neither cancellation, nor `PATCH status`, nor `dsar/erase` ever deletes `ClassRoster` rows — only a hard `Member` delete does, via `ON DELETE CASCADE`. So an erased member stays on the class allow-list, `rosterCount > 0` keeps the gate armed for everyone else, and `GET /api/classes/[id]/roster` renders "Deleted member" as an expected attendee | cascade note `lib/member-delete.ts:16-19`; erase sweep `app/api/admin/dsar/erase/route.ts:276-393` has no `classRoster` call; gate `lib/checkin.ts:160-173`; list `app/api/classes/[id]/roster/route.ts:20` | **Medium** |
| G-15 | member cancelled / suspended between kiosk search and kiosk tap | signed `kioskMemberToken`, 10-minute TTL | `POST /api/kiosk/[token]/checkin` | The status filter is applied **only at search time**. The token binds `memberId` + `tenantId` + expiry and nothing else, and `performCheckin` never re-reads `Member.status`. A member cancelled mid-session is admitted for the remaining window; more importantly a token captured from the response can be replayed for 10 minutes against any class instance | token mint `app/api/kiosk/[token]/members/route.ts:110`; verify `app/api/kiosk/[token]/checkin/route.ts:68-71`; no status read in `lib/checkin.ts:113-326` | **Low-Medium** |
| G-16 | `accountType = "kids"` | kiosk direct name search | `GET /api/kiosk/[token]/members` | The outer query has **no `parentMemberId: null` filter**, so a kid matches the search in their own right and is handed their own signed `kioskMemberToken`. The rule that kids check in via the parent tap is expressed **only** as the client-side `selfTrainable` boolean. The server accepts a kid token at `/checkin` unconditionally | outer query `app/api/kiosk/[token]/members/route.ts:56-94`; `selfTrainable` computed for display at `:104-107`; no server-side equivalent in `app/api/kiosk/[token]/checkin/route.ts` | **Low** (a client bug, not an attacker path — the kid is a legitimate member of that gym) |
| G-17 | kid promoted to adult | `membershipType` still null, `parentMemberId` now null | kiosk | Promotion makes the row invisible to **both** kiosk routes into it: it no longer appears in any parent `linkedKids` list (`children` is keyed on `parentMemberId`), and `selfTrainable` requires `!!membershipType`, which kid-creation never sets. The promoted member appears in search but with no way to select them | promote `app/api/members/[id]/promote-to-adult/route.ts:41-44`; kid creation sets no `membershipType` — `app/api/member/children/route.ts:88-100`; kiosk `selfTrainable` `app/api/kiosk/[token]/members/route.ts:104-107`; `linkedKids` `:80-90,120-129` | **Medium** |

### 1.2 Tenant lifecycle × in-flight identity and machine traffic

| # | State A | State B | Surface | Behaviour | Evidence | Severity |
|---|---|---|---|---|---|---|
| G-18 | tenant suspended or soft-deleted | member or owner requests a login code | `POST /api/auth/forgot-password`, `POST /api/magic-link/request` | Both look the tenant up by slug and check **existence only**. A soft-deleted gym keeps sending branded OTP and magic-link emails from its own `EmailLog`, and the magic link that results mints a working session (Lane A #8). `forgot-password` additionally writes a fresh `PasswordResetToken` row into a tenant that is 30 days from purge | `app/api/auth/forgot-password/route.ts:39-44` (opaque-200 on missing tenant, no state read); token write `:79-94` | **High** (KNOWN-EXT of B-A9 — A9 covers Stripe-driven email; this is member-initiated email and token minting) |
| G-19 | `stripe/disconnect` run while member subscriptions are still live | Stripe keeps delivering events for that connected account | `POST /api/stripe/webhook`, `lib/stripe/reconcile.ts` | Disconnect nulls `stripeAccountId` **and** sets `stripeConnected: false` in one write. The webhook resolves the tenant by `stripeAccountId`, finds nothing, throws `WebhookRetryableError` and returns **409 so Stripe retries — permanently**. And the reconcile sweep enumerates `{ stripeConnected: true, stripeAccountId: { not: null } }`, so it is **blind to exactly this tenant**. This is the same permanent-409 state as a purged tenant (B-A10) but on a gym that is fully alive and trading | disconnect `app/api/stripe/disconnect/route.ts:34-42`; webhook `app/api/stripe/webhook/route.ts:158-165` + 409 at `:1052-1058`; reconcile filter `lib/stripe/reconcile.ts:78-83` | **High** (KNOWN-EXT of B-B16 / B-A10 — neither records that disconnect also blinds the backstop) |
| G-20 | tenant suspended or soft-deleted | nightly reconcile sweep | `/api/cron/retention` step 1 | **Answering the open question in Lane B:** the sweep does nothing to a suspended or soft-deleted tenant. It is strictly read-only — it lists Stripe events, diffs them against `StripeEvent`, and raises Sentry. Because the webhook still fully processes a soft-deleted tenant (B-A9), nothing shows as missing, so it stays silent. It cannot see a purged tenant (row gone) or a disconnected one (G-19). Its blind spots are exactly the states that need it | `lib/stripe/reconcile.ts:14-15` ("Read-only against the DB"), `:78-83`, `:104-119`; called first at `app/api/cron/retention/route.ts:146-164` | **Medium** |
| G-21 | impersonation cookie present | `sessionVersion` bumped by suspend, soft-delete, `logout-all` or an owner password reset | `auth.ts` jwt callback | The impersonation branch does not merely defer the revocation check — it **re-copies `token.sessionVersion = target.sessionVersion` from the database on every request**, and re-stamps `sessionVersionCheckedAt`. The comparison at `:673` is therefore always self-consistent. The impersonation session cannot be evicted by any version bump at all, for the full 60-minute cookie life; the real owner is evicted within 10 minutes | re-copy `auth.ts:655`, re-stamp `:653`, comparison `:673-676`; suspend bumps `app/api/admin/customers/[id]/suspend/route.ts:114-115`; soft-delete `app/api/admin/customers/[id]/soft-delete/route.ts:101-102` | **High** (CORRECTS-C — C rank 6 says "cannot evict within the normal 10-minute window"; the true answer is "cannot evict, full stop") |
| G-22 | `Tenant.onboardingCompleted = false` | staff role is not `owner` | `/dashboard/**` | The redirect to `/onboarding` is gated on `role === "owner"`. A manager, coach or admin lands on a working dashboard whose action-item builder short-circuits to `[]` on exactly this condition. They see a permanently empty, unexplained dashboard, and cannot reach `/onboarding` (that page redirects non-owners straight back) | `app/dashboard/layout.tsx:33-35`; `app/dashboard/page.tsx:37`; `app/onboarding/page.tsx:13` | **Medium** |
| G-23 | `Tenant.onboardingCompleted = false` | operator impersonating the owner | `/dashboard` | The layout gate fires on the impersonated `role: "owner"`, so the operator is bounced into the club's setup wizard and **cannot view the dashboard at all**. Any wizard step they complete is written as the owner (C rank 2), so a support session on a brand-new tenant is indistinguishable in the audit trail from the owner doing their own setup | `app/dashboard/layout.tsx:33`; role override `auth.ts:646-647`; audit attribution `lib/audit-log.ts` via `logAudit` call sites | **Medium** |

### 1.3 Money × entitlement

| # | State A | State B | Surface | Behaviour | Evidence | Severity |
|---|---|---|---|---|---|---|
| G-24 | class pack already fully consumed (`creditsRemaining: 0`, `status: "active"`) | owner issues a refund | `POST /api/payments/[id]/refund` | The route refunds the full charge with **no check on how much of the pack was used**, no proration, and no warning in the response or the audit metadata beyond `packVoided: true`. A member who trained ten times on a ten-class pack gets 100% of their money back and the club has no signal that it happened | refundable amount derived only from `Payment` `:150-163`; pack branch `:313-322` never reads `creditsRemaining` before voiding | **Medium-High** — commercial |
| G-25 | chargeback **lost** | member subscription still live | `charge.dispute.updated` (`status: "lost"`) | The branch marks the `Payment` refunded, voids the funded pack and flips the member to `overdue` — and **never touches `stripeSubscriptionId`**. The member who charged back keeps being billed next cycle. The owner-facing refund route was given an explicit `subscriptionAction` decision for exactly this reason; the dispute path was not | dispute-lost `app/api/stripe/webhook/route.ts:924-950`, member flip `:962-969`; contrast the refund route's mandatory `subscriptionAction` `app/api/payments/[id]/refund/route.ts:127-138` | **High** — direct repeat-chargeback driver |
| G-26 | `Tenant.memberSelfBilling = false` ("we manage payments centrally") | member opens the shop or a class-pack page | `POST /api/member/checkout`, `POST /api/member/class-packs/buy` | Every **subscription** route refuses when self-billing is off. Neither the shop checkout nor the class-pack purchase checks the flag at all — both gate on `stripeConnected` only. A gym that explicitly turned member self-service billing off still takes member card payments for packs and merchandise | flag checked in `subscriptions/start:68`, `start-for-kid:72`, `cancel:43`, `cancel-for-kid:55`, `stripe/portal:39`, `family/[id]/billing/portal:40`; **absent** from `app/api/member/checkout/route.ts` and `app/api/member/class-packs/buy/route.ts` (grep `memberSelfBilling` returns no hit in either file) | **Medium-High** — an explicit owner setting is silently ignored on two money surfaces |
| G-27 | `invoice.payment_succeeded` arrives for a member already `status: "cancelled"` | out-of-order Stripe delivery | webhook | This branch writes `paymentStatus: "paid"` with **no cancelled guard**, while its sibling `payment_intent.succeeded` has one (`if (member.status !== "cancelled")`). A late final invoice therefore returns a cancelled member to `paid`, which is exactly the pair of values `hasActiveSubscription` needs — so combined with G-2 the member regains subscription-grade self check-in | unguarded `app/api/stripe/webhook/route.ts:367-370`; guarded sibling `:768-773`; coverage `lib/checkin.ts:195-196` | **Medium** |
| G-28 | BACS Direct Debit in flight (`paymentStatus: "pending"`) | member tries to train | `POST /api/checkin` (self) | `pending` is a legal CHECK value and is set by `payment_intent.processing`. `hasActiveSubscription` requires exactly `"paid"`, so for the ~4 working days a BACS payment takes to settle the member is refused with **402 "No active membership or class pack credits"** — the same message an unpaid member sees. The kiosk lets them in. Nothing tells them their payment is merely in transit | `pending` written `app/api/stripe/webhook/route.ts:590-597`; coverage `lib/checkin.ts:195-196`; 402 copy `app/api/checkin/route.ts:148-152` | **Medium** (KNOWN-EXT of B-B6, which covers `paused` and `free` but not the BACS `pending` window) |
| G-29 | `stripeAccountId` set but `stripeConnected = false` | member shop checkout | `POST /api/member/checkout` | `const connectedAccount = tenant.stripeConnected ? tenant.stripeAccountId : undefined` and the Stripe call passes `connectedAccount ? { stripeAccount } : undefined` — so in that state the checkout session is created on the **MatFlow platform account**, the member pays MatFlow, the gym is never credited, and the `checkout.session.completed` webhook never fires for a connected account so the `Order` sits `pending` forever. The state is **not reachable today**: connect and disconnect both write the two fields in a single `update` | dangerous fallback `app/api/member/checkout/route.ts:167,201`; paired writes `app/api/stripe/connect/callback/route.ts:63` and `app/api/stripe/disconnect/route.ts:40` | **Low today, Critical if the invariant ever breaks** — the guard immediately above (`:145-150`) already proves `stripeAccountId`, so the ternary is redundant and should be deleted rather than relied on |

### 1.4 Data lifecycle × identity × concurrency

| # | State A | State B | Surface | Behaviour | Evidence | Severity |
|---|---|---|---|---|---|---|
| G-30 | GDPR-erased member | live `MemberClassPack` rows | erase, reporting, pack expiry | `dsar/erase` scrubs the `Member` row and eight dependent tables but **never touches `MemberClassPack`, `ClassPackRedemption`, `ClassRoster` or `AttendanceRecord`**. The erased member keeps `active` packs with credits on the books. They are inert for check-in only because the member can no longer authenticate — the entitlement itself was never revoked, and the club's deferred-revenue reporting still counts it | erase sweep `app/api/admin/dsar/erase/route.ts:276-393` — no `memberClassPack`, `classPackRedemption`, `classRoster` or `attendanceRecord` call; contrast the hard-delete cascade `lib/member-delete.ts:109-121` which removes all four | **Medium** |
| G-31 | owner edits or clears `waiverTitle` / `waiverContent` | members who already signed | settings, waiver, check-in | Nothing versions, nothing re-signs, nothing warns. `SignedWaiver.version` is `@default(1)` and has **zero writers and zero readers** in the entire repo — every signature is version 1 forever. `Member.waiverAccepted` is never reset. The zod schema is `.optional().nullable()`, so an owner can also **delete** the custom waiver, silently reverting all future signatures to the built-in default. The only record of which text a member agreed to is `contentSnapshot`, which Lane D proved has **no UI anywhere** | schema `prisma/schema.prisma:666`; grep for a `version:` assignment across the four `signedWaiver.create` sites (`waiver/open:109`, `waiver/sign:100`, `waiver/sign-for-child:121`, `members/[id]/waiver/sign:103`) returns none; nullable settings field `app/api/settings/route.ts:37-38`; no reader of `SignedWaiver.version` anywhere | **High** — a club that tightens its waiver after an injury cannot force a re-sign and cannot prove who signed which text |
| G-32 | member holds a magic-link session (no `memberId` claim) | member presses "sign out everywhere" | `POST /api/auth/logout-all` | `session.user.memberId` is undefined on that session, so the handler takes the `else` branch and runs `tx.user.update({ where: { id: session.user.id } })` with a **Member id**. Prisma raises P2025, the catch fires, and the member gets **500 "Failed to revoke sessions"** | branch `app/api/auth/logout-all/route.ts:23-36`, catch `:49-51`; the omitted claim `app/api/magic-link/verify/route.ts:98-107` | **Medium** (CORRECTS-A — A rank 1 lists `logout-all` as a silent no-op against that session; it is in fact a hard 500) |
| G-33 | two owners commit the same `ImportJob` | 200-row CSV | `POST /api/admin/import/[id]/commit` | The status check is a plain read (`if (job.status === "running" or "complete") 409`) followed by a separate unguarded `update` — a check-then-set, not the guarded `updateMany` pattern this codebase uses everywhere else (refund ledger, pack redemption, Stripe customer create). Two concurrent commits both pass, both import. Member inserts are idempotent via `createMany({ skipDuplicates: true })` and `@@unique([tenantId, email])`, so the damage is doubled row counters, a double blob `del()` and two "import complete" emails | `app/api/admin/import/[id]/commit/route.ts:23-36`; contrast `app/api/payments/[id]/refund/route.ts:296-312` and `lib/checkin.ts:221-229` | **Low-Medium** |
| G-34 | `bulk-invite` loop dies mid-run (Resend cap, function timeout) | 200 candidates, `maxDuration = 300` | invite day | The token is minted **and committed** before each send, so a mid-loop failure leaves a set of valid 7-day `first_time_signup` tokens whose links were never delivered and whose raw values were never returned. Re-running is the only recovery, and the re-run's first act is `magicLinkToken.updateMany({ used: true })` — invalidating exactly the tokens that were fine. Every partial run therefore resets the whole cohort | mint-then-send `app/api/members/bulk-invite/route.ts:84-106`; invalidation `:86-89`; response withholds the raw token `:133` | **Medium** (KNOWN-EXT of D-G2 — G2 records that the token never leaves the function; the new part is that a partial run is not resumable, only restartable) |
| G-35 | erased member's old Stripe customer / subscription id | any customer-keyed webhook (`invoice.payment_failed`, `customer.subscription.updated`, `payment_intent.succeeded`, `mandate.updated`, `payment_method.detached`) | webhook | **Answering the question directly: silent skip, no crash, no row re-creation.** `findMember` keys on `stripeCustomerId`, which the erase nulled, so it returns null and every one of those branches no-ops and acks 200. The two paths that *do* still reach an erased member are the `metadata.memberId` class-pack branch (G-6) and the `linkedPayment.memberId` dispute fallback (G-7) | `findMember` `app/api/stripe/webhook/route.ts:166-186`; erase nulls both ids `app/api/admin/dsar/erase/route.ts:297-299` | **Informational** |
| G-36 | hard-deleted member | dispute arrives on their old charge | webhook | Also safe, but by a different mechanism: `Payment.memberId` is `ON DELETE SET NULL`, so `linkedPayment?.memberId` is null and `disputeMemberId` resolves to null — the `member.update` is skipped and the audit row falls back to `entityType: "Dispute"`. The asymmetry is worth stating: **hard delete is safe here and GDPR erase is not**, because erase leaves the FK intact | `lib/member-delete.ts:20-21`; fallback `app/api/stripe/webhook/route.ts:962-969,979-981` | **Informational** |

### 1.5 Second factor × alternate credential paths

| # | State A | State B | Surface | Behaviour | Evidence | Severity |
|---|---|---|---|---|---|---|
| G-37 | owner sitting on `/login/totp` with `totpPending: true` | a magic link already in their inbox | `GET /api/magic-link/verify` | `totpPending` exists **only inside the JWT** — there is no server-side "challenge in progress" record. The magic-link route mints a second cookie with `totpPending` hardcoded `false` and overwrites the first. The proxy's `totpPending` redirect cannot intervene because `api/magic-link` is excluded from the middleware matcher entirely. The 2FA challenge is not bypassed so much as **overwritten mid-flight** | hardcoded `app/api/magic-link/verify/route.ts:86,96,106`; cookie overwrite `:128-134`; proxy redirect `proxy.ts:187-189`; matcher exclusion `proxy.ts:224` | **Medium** (KNOWN-EXT of A #8 / B-A7 — those record that magic link ignores state; the new part is that an *in-progress* challenge has no server-side existence to protect) |
| G-38 | owner sitting on `/login/totp` | completes a password reset in another tab | `POST /api/auth/reset-password` | The reset bumps `sessionVersion`, which invalidates the half-finished TOTP-pending JWT on the next Node-runtime `auth()` call. The owner is returned to `/login`, signs in with the new password, and is challenged for TOTP again — so this combination is **safe**, but only as a side effect of the version bump, not by design. There is no explicit "abandon the pending challenge" path | bump `app/api/auth/reset-password/route.ts:160-166`; check `auth.ts:668-679`; re-challenge `auth.ts:335` | **Informational — FINE** |
| G-39 | member enrolled in TOTP (A DE-1 "member 2FA bricks the account") | magic link | `/api/magic-link/verify` | The bricked-member trap has an escape hatch nobody documents: magic-link verify sets `totpPending: false` and never consults `Member.totpEnabled`, so a member who bricked themselves with 2FA can still get in via a magic link — and only via a magic link, forever | `app/api/magic-link/verify/route.ts:47-54,106`; the trap itself `proxy.ts:187` + `app/api/auth/totp/verify/route.ts:42-51` | **Informational** (mitigation for A rank 2, worth recording because it is the only one) |


### 1.6 Email deliverability state × identity state

| # | State A | State B | Surface | Behaviour | Evidence | Severity |
|---|---|---|---|---|---|---|
| G-40 | recipient address is inside the 30-day bounce suppression window | staff re-run `bulk-invite` for that member | invite day | `sendEmail` short-circuits and returns `{ ok: false }` — but the route has **already** minted a new token and, one statement earlier, marked every previous unused `first_time_signup` token for that address `used: true`. So each re-run destroys the one link that might still have been in the member's inbox and replaces it with one that was never sent. Repeated attempts strictly worsen the position, and the raw token is never returned | invalidate-then-mint `app/api/members/bulk-invite/route.ts:86-99`; send after the write `:101-110`; suppression `lib/email.ts:350-368` | **Medium-High** — this is the single most likely first-day failure for a CSV-imported club |
| G-41 | member hard-bounced (address suppressed tenant-wide) | that member is then GDPR-erased | future email to the same address | The erase rewrites `EmailLog.recipient` to `deleted-<id>@deleted.invalid` for **every** historical row including the `bounced` one. `sendEmail`'s suppression lookup matches on `recipient`, so the real address is silently un-suppressed by the erasure. If anyone re-enters that address later the club resumes emailing a known-bad inbox with no record of why it was blocked | erase rewrite `app/api/admin/dsar/erase/route.ts:382-393`; suppression predicate `lib/email.ts:355-368` | **Low-Medium** |
| G-42 | bounce webhook event arrives after the erase | `EmailLog.resendId` still matches | `POST /api/webhooks/resend` | The webhook keys on `resendId` under `withRlsBypass`, so it still lands and writes `status: "bounced"` — but onto a row whose `recipient` is now the sentinel. The suppression is therefore registered against `deleted-…@deleted.invalid` rather than the address that actually bounced. Same net effect as G-41, reached from the other direction | `app/api/webhooks/resend/route.ts:123-146` | **Low** |

---

## 2. Cells I probed that are FINE

Recorded so the loop does not re-spend effort on them.

| Combination | Why it is fine | Evidence |
|---|---|---|
| **Parent's Stripe customer × kid's Stripe customer** — could a failed kid invoice flip the parent to `overdue`, or vice versa, via `findMember`'s `findFirst`? | **No.** Every kid gets their **own** Stripe customer, created against the kid's `Member.id`, so `stripeCustomerId` is never shared between a parent and a child and `findFirst` cannot pick the wrong row | `app/api/member/subscriptions/start-for-kid/route.ts:131-140`; find-or-create keyed on `member.id` `lib/stripe/subscriptions.ts:78-98` |
| **Member with two live subscriptions on one customer × `customer.subscription.deleted`** | Correctly scoped: the `updateMany` predicate includes `stripeSubscriptionId: deletedSubId`, so an unrelated subscription's deletion cannot cancel the member | `app/api/stripe/webhook/route.ts:226-239` |
| **Two simultaneous check-ins for one member with one credit left** (self, kiosk, or one of each) | Atomic. Both paths use `updateMany` guarded on `creditsRemaining: { gt: 0 }` and treat `count === 0` as "no coverage", so the pack cannot go negative | `lib/checkin.ts:221-229` and `:297-301` |
| **Two simultaneous refunds of one payment** | Atomic. `updateMany` guarded on the exact `refundedAmountPence` read, with a distinct 409 and a CRITICAL log carrying the Stripe refund id | `app/api/payments/[id]/refund/route.ts:296-312,330-351` |
| **Out-of-order `customer.subscription.updated` resurrecting a cancelled member** | Explicitly guarded — `wouldResurrectCancelled` blocks any non-cancel-direction update on an already-cancelled member | `app/api/stripe/webhook/route.ts:685-698` |
| **Staff undo of a check-in × pack credit** | The credit is restored inside the same transaction as the attendance delete, and the orphaned `ClassPackRedemption` row goes with it. Refunded packs deliberately do not get the credit back; expired packs do (inert, since expiry is re-checked at redemption) | `app/api/checkin/route.ts:192-199`; `lib/checkin.ts:68-111` |
| **Tenant purge × `Task` rows** (not in the purge's `tenantScoped` list) | Safe by FK: `Task.tenant` is `onDelete: Cascade`, and `Task.assigneeMemberId` / `createdById` / `assignedToId` are all `SetNull`, so the member and user deletions earlier in the purge cannot hit P2003 and the rows disappear with the tenant row | `prisma/schema.prisma:1074,1083-1093`; purge list `app/api/cron/retention/route.ts:633-653`; tenant delete step 8 `:686-700` |
| **GDPR-erased member × supervised waiver link (`purpose: "waiver_open"`)** | Cannot fire. The route resolves the member by `tokenRow.email`, which no longer matches after the sentinel overwrite, and the erase deletes all `MagicLinkToken` rows for the original address case-insensitively anyway | `app/api/waiver/open/route.ts:103-107`; erase token cleanup `app/api/admin/dsar/erase/route.ts:375-380` |
| **`charge.refunded` webhook replay / partial-then-full sequence** | Idempotent: the `status !== "refunded"` guard skips exhausted charges, and `refundedAmountPence` is overwritten with Stripe's own cumulative `amount_refunded`, so replays converge rather than accumulate | `app/api/stripe/webhook/route.ts:624-637` |
| **Password reset completed while a TOTP challenge is pending** | Safe (see G-38) — the `sessionVersion` bump kills the half-finished JWT and the fresh login is re-challenged |  `app/api/auth/reset-password/route.ts:160-166`; `auth.ts:335,668-679` |
| **Kid created by a parent × cross-parent or cross-tenant access** | Every kid-scoped route uses the composite predicate `{ id, tenantId, parentMemberId }` and answers 404 rather than 403, so neither existence nor ownership leaks | `app/api/checkin/route.ts:88-94`; `app/api/member/subscriptions/start-for-kid/route.ts:99-113`; `app/api/member/family/[id]/billing/route.ts:44-48` |
| **`ImportJob` mid-preview × tenant soft-deleted** | The commit and preview routes are `requireApiOwner`, and the owner cannot authenticate once `deletedAt` is set (`auth.ts:193`), so nothing can advance. The job row and blob are then removed by the purge (step 6) or, earlier, by the 30-day abandoned-import rule already recorded as D-G14 | `app/api/admin/import/[id]/commit/route.ts:19`; `app/api/cron/retention/route.ts:667-677` |
| **Staff check-in picker × cancelled or erased member** | The picker filters `status IN (active, taster)`, so an erased member is not offered. (The raw `POST /api/checkin` with a known `memberId` has no status filter — but that requires already knowing the id, and the admin profile is a deliberate override) | `app/api/checkin/members/route.ts:35-36`; override rationale `lib/checkin.ts:9` |

---

## 3. Cells I could not resolve statically

Each needs a runtime probe. None was run — this audit is read-only.

| # | Question | Why static reading cannot answer it | Cheapest probe |
|---|---|---|---|
| U-1 | Does Resend actually hard-bounce `@no-login.matflow.local` (G-5, G-11), or reject it at the API with a validation error before it ever counts as a bounce? | The distinction decides whether the consequence is domain-reputation damage plus a `bounced` `EmailLog` row, or a clean `failed` row with no reputation cost. `lib/email.ts:405-421` records either outcome the same way | One `sendEmail` to a `.local` address in a staging tenant; read the resulting `EmailLog.status` and `errorMessage` |
| U-2 | Is `RESEND_WEBHOOK_SECRET` actually set in production, and is the Resend endpoint actually registered? | The bounce webhook exists, is svix-signature-verified, and writes `bounced` / `complained` correctly (`app/api/webhooks/resend/route.ts:87-101,133-146`). But if the secret is unset the route **503s every event in production** (`:44-49`), so the entire 30-day suppression at `lib/email.ts:350-368` — and with it D-0.12 and D-G3 — would be silently dormant with no signal anywhere | Check the deployed env for `RESEND_WEBHOOK_SECRET` and the Resend dashboard for the endpoint; then `SELECT status, count(*) FROM "EmailLog" GROUP BY 1` and look for any `bounced` row at all |
| U-3 | Does a `checkout.session.completed` for a class pack actually arrive after a DSAR erase in practice (G-6)? | Depends on whether `dsar/erase`'s fail-closed Stripe step (`:112-145`) also voids open checkout sessions. It cancels subscriptions; open one-off checkout sessions are a different object and I found no `checkout.sessions.expire` call anywhere | Create a pack checkout session, erase the member before paying, then pay. Inspect for a new `MemberClassPack` row |
| U-4 | Is the impersonation token override (G-21) re-applied after the 60-minute cookie expires, or does the token keep the impersonated identity? | `auth.ts:632-660` mutates the token in place with **no revert branch**. Once the cookie is gone the override simply stops running, but the previously-written `token.id` / `token.tenantId` / `token.role` are already persisted in the JWT. Whether NextAuth v5 re-issues from the mutated token or from the original is runtime behaviour | Start an impersonation, wait past 3600s, then hit any `/dashboard` page and read `session.user.id`. C's open question §5.1 asks the same thing |
| U-5 | Does `Member.status = "cancelled"` set by the webhook race the staff PATCH that set it, double-stamping `cancelledAt`? | Both writers stamp `cancelledAt: new Date()` and the webhook's `updateMany` has no "already cancelled" guard (only `customer.subscription.updated` has one, via `wouldResurrectCancelled`). Whether the churn date ends up as the staff action or the period end depends on delivery timing | Cancel a member with an active subscription via `PATCH`, then let the period-end `customer.subscription.deleted` land; compare `cancelledAt` before and after |
| U-6 | Do the CHECK constraints named in `20260430000001` exist in the deployed Neon database? | Read from migration SQL only; `NOT VALID` then `VALIDATE` means a failed validation would have aborted the migration, but I did not query the database. Everything in G-12 and G-28 about the legal `paymentStatus` set rests on this file | `\d+ "Member"` against the deployed database |
| U-7 | Is `Tenant.currency` ever anything but GBP in practice, given C-12 says it is settable by no route? | G-1 and G-24 quote money in pounds throughout; if a tenant row were hand-set to EUR the refund route's `formatMoney` handles it but the class-pack `ClassPack.currency` default (`"GBP"`, `prisma/schema.prisma:791`) would diverge | Read `SELECT DISTINCT currency FROM "Tenant"` |
| U-8 | Does the kiosk client actually offer a check-in button for a search result whose `selfTrainable` is false and whose `linkedKids` is empty (G-16, G-17)? | The server hands out a usable token regardless; whether the UI exposes it is in `app/kiosk/**`, which I did not read line by line | Open a kiosk URL and search for a promoted-to-adult member with no `membershipType` |

---

## 4. Method and coverage

- Every cell was reached by reading the source. No dev server, test suite,
  database, Stripe call or email send was executed, and no file in
  `C:/Users/NoeTo/Desktop/matflow` was modified.
- The probe list in the brief was worked through in order. Cells the four prior
  lanes already cover are omitted; where a prior lane's wording is materially
  wrong the row is tagged **CORRECTS-x** (G-21 corrects C rank 6; G-32 corrects
  A rank 1) and where the consequence differs it is tagged **KNOWN-EXT**.
- Two brief items resolved to "nothing to find": **waitlist spots surviving a
  cancellation** is vacuous because `ClassWaitlist` has no writer (G-3), and
  **row re-creation from a webhook for an erased customer id** does not happen
  through the customer-keyed branches (G-35) — only through the two
  member-id-keyed ones (G-6, G-7).
- One prior-lane claim is now stale and worth flagging to whoever owns Lane B:
  **B-B4 states "the refund route has zero `stripeSubscriptionId` references"**.
  It has several — `subscriptionAction` is a required, three-valued, explicit
  decision for any invoice-backed payment
  (`app/api/payments/[id]/refund/route.ts:41-44,127-138,251-273`). The gap that
  remains is the **dispute-lost** path (G-25), not the refund path.

*End of Lane G. Sections 0 to 4 complete. All paths are repository-root
relative.*
